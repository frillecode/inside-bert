model: "roberta-base"
task: "MRPC"

timestamp: "2024-08-23_14-39-18"

output_dir: "/Volumes/Expansion" #/work/Results

window: XX # referring to n_docs, not n_steps!!!
step_cutoff: XX

training_args:
  per_device_train_batch_size: 8
  per_device_eval_batch_size: 8
  num_train_epochs: 3
  logging_dir: "logs"
  logging_steps: 10
  evaluation_strategy: "steps"
  save_steps: 10